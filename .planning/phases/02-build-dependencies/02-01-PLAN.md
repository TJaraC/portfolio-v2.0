---
phase: 02-build-dependencies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/ui/ImageWithCurtain.tsx
  - src/hooks/useCarouselScroll.ts
  - src/hooks/useResponsiveAnimations.ts
autonomous: true
requirements:
  - BUILD-01

must_haves:
  truths:
    - "npx tsc --noEmit exits with code 0 (zero type errors)"
    - "No LenisOptions property errors in App.tsx"
    - "No NodeJS namespace errors in hooks"
    - "No RefObject assignability errors in ImageWithCurtain.tsx"
  artifacts:
    - path: "src/App.tsx"
      provides: "Lenis initialization without normalizeWheel"
    - path: "src/components/ui/ImageWithCurtain.tsx"
      provides: "Correctly typed ref as RefObject<HTMLDivElement>"
    - path: "src/hooks/useCarouselScroll.ts"
      provides: "ReturnType<typeof setTimeout> instead of NodeJS.Timeout"
    - path: "src/hooks/useResponsiveAnimations.ts"
      provides: "ReturnType<typeof setTimeout> instead of NodeJS.Timeout"
  key_links:
    - from: "tsconfig.json"
      to: "src/**/*.ts(x)"
      via: "tsc --noEmit"
      pattern: "noEmit.*true"
---

<objective>
Fix all 4 TypeScript compiler errors so that `tsc --noEmit` exits with code 0. This is the prerequisite for switching the build script from `tsc --noCheck` to `tsc --noEmit` in Plan 03 (BUILD-01).

Purpose: The production build currently bypasses type checking entirely. Before the build script can be fixed, the codebase must be type-correct.
Output: 4 source files updated with correct TypeScript types; `npx tsc --noEmit` exits 0.
</objective>

<execution_context>
@E:/tricio_s_application/.claude/get-shit-done/workflows/execute-plan.md
@E:/tricio_s_application/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix NodeJS.Timeout namespace errors in hooks</name>
  <files>src/hooks/useCarouselScroll.ts, src/hooks/useResponsiveAnimations.ts</files>

  <read_first>
    - src/hooks/useCarouselScroll.ts (see current NodeJS.Timeout usage)
    - src/hooks/useResponsiveAnimations.ts (see current NodeJS.Timeout usage)
  </read_first>

  <action>
In both hook files, find every variable typed as `NodeJS.Timeout` and change it to `ReturnType<typeof setTimeout>`. This removes the dependency on the `NodeJS` namespace which is not available in the browser-targeted `lib` config (`ES2020`, `DOM`, `DOM.Iterable`).

Specific changes:

In `src/hooks/useCarouselScroll.ts` line 15:
- Find: `NodeJS.Timeout` (used as a type annotation, e.g. `useRef<NodeJS.Timeout | null>` or `let timer: NodeJS.Timeout`)
- Replace with: `ReturnType<typeof setTimeout>`

In `src/hooks/useResponsiveAnimations.ts` line 98:
- Find: `NodeJS.Timeout` (same pattern)
- Replace with: `ReturnType<typeof setTimeout>`

Do NOT change any logic, variable names, or other code. Only the type annotation changes.
  </action>

  <verify>
    <automated>cd E:/tricio_s_application && npx tsc --noEmit 2>&1 | grep -E "NodeJS|useCarouselScroll|useResponsiveAnimations"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "NodeJS" src/hooks/useCarouselScroll.ts` returns no results
    - `grep -n "NodeJS" src/hooks/useResponsiveAnimations.ts` returns no results
    - `grep -n "ReturnType" src/hooks/useCarouselScroll.ts` returns at least 1 result
    - `grep -n "ReturnType" src/hooks/useResponsiveAnimations.ts` returns at least 1 result
    - Running `npx tsc --noEmit 2>&1 | grep "useCarouselScroll\|useResponsiveAnimations"` returns no lines
  </acceptance_criteria>

  <done>Both hooks compile without NodeJS.Timeout errors. Zero TS2503 errors.</done>
</task>

<task type="auto">
  <name>Task 2: Fix LenisOptions and RefObject type errors in App.tsx and ImageWithCurtain.tsx</name>
  <files>src/App.tsx, src/components/ui/ImageWithCurtain.tsx</files>

  <read_first>
    - src/App.tsx (see current Lenis initialization with normalizeWheel)
    - src/components/ui/ImageWithCurtain.tsx (see current ref type annotation)
  </read_first>

  <action>
Fix 2 remaining type errors:

**Error 1 — src/App.tsx line 38: `normalizeWheel` does not exist in `LenisOptions`**
Find the Lenis constructor call (e.g. `new Lenis({ ..., normalizeWheel: true, ... })` or `lenis({ ..., normalizeWheel: ..., ... })`).
Remove ONLY the `normalizeWheel` property from the options object. Do not change any other properties, logic, or surrounding code. The `normalizeWheel` option was removed from Lenis v1.x API.

**Error 2 — src/components/ui/ImageWithCurtain.tsx line 78: `RefObject<HTMLElement>` not assignable to `RefObject<HTMLDivElement>`**
Find the ref declaration: it is typed as `useRef<HTMLElement>` or `RefObject<HTMLElement>` but passed to a `<div>` element's `ref` prop.
Change the type argument from `HTMLElement` to `HTMLDivElement`. Example:
- Before: `const containerRef = useRef<HTMLElement>(null)`
- After: `const containerRef = useRef<HTMLDivElement>(null)`

Do NOT change any other code in either file.
  </action>

  <verify>
    <automated>cd E:/tricio_s_application && npx tsc --noEmit 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` exits with code 0 (no output, or output with zero error lines)
    - `grep -n "normalizeWheel" src/App.tsx` returns no results
    - `grep -n "useRef<HTMLElement>" src/components/ui/ImageWithCurtain.tsx` returns no results (replaced by HTMLDivElement)
    - Running `npx tsc --noEmit 2>&1 | grep "error TS"` returns zero lines
  </acceptance_criteria>

  <done>All 4 TypeScript errors resolved. `npx tsc --noEmit` exits 0 with no error output. The codebase is type-correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build toolchain | tsc + vite run on developer machine / CI — trusted environment, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | TypeScript type annotations | accept | Type-only changes; no runtime behavior affected; reviewed via `tsc --noEmit` output |
| T-02-01-02 | Information Disclosure | Source code (no PII) | accept | No secrets in modified files; type annotation changes only |
</threat_model>

<verification>
After both tasks complete:

```bash
cd E:/tricio_s_application && npx tsc --noEmit 2>&1
```

Must produce no output (exit 0). If any lines contain `error TS`, tasks are not done.

Also verify the build still works (Vite part):
```bash
cd E:/tricio_s_application && npm run build 2>&1 | tail -5
```
</verification>

<success_criteria>
1. `npx tsc --noEmit` exits 0 with zero error lines — all 4 TS errors resolved.
2. `npm run build` still succeeds (Vite build, not yet with `tsc --noEmit` in script — that is Plan 03).
3. No logic changes to any source file — only type annotations modified.
</success_criteria>

<output>
After completion, create `.planning/phases/02-build-dependencies/02-01-SUMMARY.md`
</output>
