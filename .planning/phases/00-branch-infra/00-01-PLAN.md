---
phase: 00-branch-infra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02

must_haves:
  truths:
    - "La rama fix/audit-2026-04 existe y esta checked out"
    - ".env y .env.local estan cubiertos por .gitignore"
    - "dist/ y .planning/ siguen cubiertos por .gitignore"
    - "Variantes Vite .env.*.local estan cubiertos por .gitignore"
  artifacts:
    - path: ".gitignore"
      provides: "Reglas de ignore para secrets y artefactos"
      contains: ".env"
  key_links: []
---

<objective>
Verificar que la rama de trabajo existe y reforzar `.gitignore` con entradas para archivos de entorno (`.env`, `.env.local`, `.env.*.local`) para que ningun secret pueda ser commitado accidentalmente.

Purpose: Fundacion de seguridad para todo el milestone — sin esto, commits posteriores podrian exponer secrets.
Output: `.gitignore` actualizado con cobertura completa de archivos de entorno.
</objective>

<execution_context>
@E:/tricio_s_application/.claude/get-shit-done/workflows/execute-plan.md
@E:/tricio_s_application/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-branch-infra/00-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify branch exists and is checked out (INFRA-01)</name>
  <files></files>
  <read_first>.gitignore</read_first>
  <action>
Run the following commands to verify the branch state per D-01:

```bash
git branch --show-current
git branch --list fix/audit-2026-04
```

The current branch must be `fix/audit-2026-04`. Per D-01, this branch already exists and is checked out — no creation or switching needed. If for any reason it is NOT checked out, run `git checkout fix/audit-2026-04`.

No files are modified in this task — it is verification only.
  </action>
  <verify>
    <automated>git branch --show-current | grep -q "fix/audit-2026-04" && echo "PASS: branch verified" || echo "FAIL: wrong branch"</automated>
  </verify>
  <acceptance_criteria>
- `git branch --show-current` outputs exactly `fix/audit-2026-04`
- `git branch --list fix/audit-2026-04` returns a non-empty result
  </acceptance_criteria>
  <done>Branch fix/audit-2026-04 confirmed as current working branch.</done>
</task>

<task type="auto">
  <name>Task 2: Add env file patterns to .gitignore (INFRA-02)</name>
  <files>.gitignore</files>
  <read_first>.gitignore</read_first>
  <action>
Read the current `.gitignore` file. It currently has sections with comments (`# Node modules`, `# Production build`, etc.) and already contains `.planning/` and `dist/` at the bottom.

Add a new section BEFORE the `.planning/` and `dist/` lines (or at an appropriate location following the comment-section pattern). The exact lines to add are:

```
# Secrets / environment
.env
.env.local
.env.*.local
```

Per D-02: `.env` and `.env.local` must be added.
Per D-03: `.env.*.local` covers Vite convention variants (`.env.development.local`, `.env.production.local`, etc.).

The existing entries (`.planning/`, `dist/`, etc.) must remain intact. The `.claude/` directory entry should also remain if present.

Do NOT remove or reorder any existing entries.
  </action>
  <verify>
    <automated>git check-ignore -v .env && git check-ignore -v .env.local && git check-ignore -v .env.production.local && git check-ignore -v dist/index.html && git check-ignore -v .planning/PROJECT.md && echo "ALL PATTERNS VERIFIED"</automated>
  </verify>
  <acceptance_criteria>
- `.gitignore` contains the line `.env` (exact, on its own line)
- `.gitignore` contains the line `.env.local` (exact, on its own line)
- `.gitignore` contains the line `.env.*.local` (exact, on its own line)
- `.gitignore` still contains `dist/` (not removed)
- `.gitignore` still contains `.planning/` (not removed)
- `git check-ignore -v .env` prints a line referencing `.gitignore`
- `git check-ignore -v .env.local` prints a line referencing `.gitignore`
- `git check-ignore -v dist/index.html` prints a line referencing `.gitignore`
- `git check-ignore -v .planning/PROJECT.md` prints a line referencing `.gitignore`
  </acceptance_criteria>
  <done>.gitignore updated with .env, .env.local, and .env.*.local entries. All four success criteria from the roadmap pass: branch exists, .env ignored, dist/ ignored, .planning/ ignored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer workstation -> git repo | Secrets in .env files must not cross into version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Information Disclosure | .env files | mitigate | Add .env, .env.local, .env.*.local to .gitignore so secrets cannot be committed |
| T-00-02 | Information Disclosure | .planning/ directory | accept | Already in .gitignore — no action needed |
| T-00-03 | Information Disclosure | dist/ build output | accept | Already in .gitignore — no action needed |
</threat_model>

<verification>
Run all four roadmap success criteria:

1. `git branch --list fix/audit-2026-04` — returns the branch name
2. `git check-ignore -v .env` — prints `.gitignore`
3. `git check-ignore -v dist/index.html` — prints `.gitignore`
4. `git check-ignore -v .planning/PROJECT.md` — prints `.gitignore`

All four must pass.
</verification>

<success_criteria>
1. Branch `fix/audit-2026-04` is checked out
2. `.gitignore` contains `.env`, `.env.local`, and `.env.*.local`
3. `git check-ignore` confirms coverage for `.env`, `dist/`, and `.planning/`
4. No existing `.gitignore` entries were removed or broken
</success_criteria>

<output>
After completion, create `.planning/phases/00-branch-infra/00-01-SUMMARY.md`
</output>
